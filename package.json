{
  "name": "net-topology-view",
  "version": "0.1.0",
  "private": true,
  "description": "全网业务态势 3D 可视化（分层星空风格）",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
